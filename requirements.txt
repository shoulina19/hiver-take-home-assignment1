openai==1.35.3
pandas==2.2.2
numpy==1.26.4
tqdm==4.66.4
python-dotenv==1.0.1
tabulate==0.9.0
# scikit-learn and scipy are NOT required — all metrics (F1, Cohen's κ,
# bootstrap CI) are implemented from scratch in src/metrics.py using stdlib only.
